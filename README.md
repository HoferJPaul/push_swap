# push_swap
push_swap is a sorting algorithm project that sorts a stack of integers using only specific operations (push, swap, rotate, reverse). The goal is to produce the shortest possible sequence of operations to sort the data efficiently, using custom logic and algorithmic optimization in C.
